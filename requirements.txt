# Core web framework dependencies
fastapi>=0.104.1
pydantic>=2.5.0
uvicorn>=0.24.0
python-multipart>=0.0.6

# RAG Pipeline dependencies
ollama>=0.1.8
pymilvus>=2.3.0   # Milvus vector database
numpy>=1.26.4

# Document processing
PyPDF2>=3.0.1
python-docx>=0.8.11

# Optional ML dependencies (used with fallbacks)
sentence-transformers  # Optional for embeddings
scikit-learn>=1.0.0    # Optional for cosine similarity

# Development dependencies
pytest>=6.0.0

# Feast feature store with Milvus support
feast[milvus]>=0.51.0
marshmallow<4.0.0  # Required for Feast Milvus compatibility

# Additional dependencies that may be needed
python-dotenv>=1.0.0  # For environment variables (if using .env files)
pandas
pydantic-settings
