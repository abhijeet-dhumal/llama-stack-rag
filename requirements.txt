# Core API dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0
pydantic==2.5.0

# LLM and embeddings
ollama==0.1.8

# Document processing - simpler approach without docling
PyPDF2==3.0.1
python-docx==0.8.11
numpy==1.26.4

# Vector database
chromadb==0.4.17

# Utilities
redis==5.0.1
requests

# Optional heavy ML dependencies (install manually for local development if needed):
# sentence-transformers==2.7.0  # 2GB! Enables semantic chunking, falls back to simple chunking
# scikit-learn==1.3.2          # 500MB! Enables sklearn cosine similarity, falls back to numpy 